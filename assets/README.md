# assets/

Ressources statiques embarquees dans l'application (icones, polices, images par defaut).
Les fichiers sont ajoutes a une cible `qt_add_resources` au fur et a mesure des besoins.

- `icons/` — icone applicative (PNG multi-tailles, `.ico` Windows, `.icns` macOS)
